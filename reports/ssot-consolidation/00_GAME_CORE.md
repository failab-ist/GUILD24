# 00_GAME_CORE consolidation ledger

BASELINE=0ee8bbb
TARGET=design_ssot/00_GAME_CORE_v2.8.0.md
CHAIN=design_ssot/00_GAME_CORE_v2.8.0.md,design_ssot/history/00_GAME_CORE_v2.5.0.md

Placement: v2.5 sections in their order; the v2.8 INFORMATION PRINCIPLE override directly after the
v2.5 principle it amends; v2.8-only sections after RUN GOAL; the two RELATED lists joined.

## Inheritance pointer — the base is now inline (v2.8)

```text
## INHERITANCE
All unchanged core fantasy, phase loop, information principle, NPC emotional core and Run identity
inherit 00_GAME_CORE_v2.5.0.md.
All other unchanged information-principle rules inherit the base document.
```

## Superseded by v2.8 INFORMATION PRINCIPLE — v2.8 OVERRIDE (v2.5 hidden list)

The v2.8 "Keep hidden" list replaces these three v2.5 items: success probability is restated
(`exact expedition success probability`); Death probability is narrowed to the *unconditional
whole-expedition* probability, with SALE allowed to expose the failure-conditioned Death risk;
internal coefficients gain `unless a current owner explicitly exposes them`. Keeping the
unqualified v2.5 items would contradict the override. `최종 정답을 대신 계산하는 단일 안전점수`
is not covered by the override and stays.

```text
- 정확한 성공확률
- 정확한 사망확률
- 내부 계수/Threshold
```

## Heading merged — v2.5 RELATED OWNERSHIP lines kept under v2.8 RELATED

```text
## RELATED OWNERSHIP
```
