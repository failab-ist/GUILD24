# 00_GAME_CORE consolidation ledger

BASELINE=0ee8bbb
TARGET=design_ssot/00_GAME_CORE_v2.8.0.md
CHAIN=design_ssot/00_GAME_CORE_v2.8.0.md,design_ssot/history/00_GAME_CORE_v2.5.0.md

Current-spec consolidation. The pre-consolidation v2.8 patch is kept whole as
`design_ssot/history/00_GAME_CORE_v2.8.0-patch.md`; the v2.5 base stays in `history/`.

## LEGACY — inheritance pointers (the base is now inline)

```text
## INHERITANCE
All unchanged core fantasy, phase loop, information principle, NPC emotional core and Run identity
inherit 00_GAME_CORE_v2.5.0.md.
All other unchanged information-principle rules inherit the base document.
The inherited information principle is updated only at the Death-risk boundary.
```

## LEGACY — v2.8 release framing / scope note

The loop and the design-goal list stay (now in CORE FUN) without the release framing; the v2.9+
deferral is routed by SPEC_INDEX.

```text
## v2.8 CORE PURPOSE
v2.8 strengthens the existing core loop rather than broadening feature count:
P2+ structural expansion remains v2.9+.
```

## REWORD — principle labels without the v2.8 framing (independent review: these are binding)

```text
Primary principle remains:
Therefore v2.8:
```

```new
Primary principle:
Therefore the design:
```

## SUPERSEDED — v2.5 loop line, replaced by the v2.8 six-step loop (now in CORE FUN)

```text
`관찰 → 추론 → 선택 → 결과 확인 → 다음 선택에 반영`
```

## SUPERSEDED — v2.5 hidden list, replaced by the v2.8 list (merged into 숨기는 것)

Success probability is restated; Death probability is narrowed to the unconditional whole-expedition
probability (SALE may expose the failure-conditioned risk); internal coefficients gain "unless a
current owner explicitly exposes them". The v2.8 list is merged under the v2.5 `숨기는 것:` label.

```text
- 정확한 성공확률
- 정확한 사망확률
- 내부 계수/Threshold
## INFORMATION PRINCIPLE — v2.8 OVERRIDE
Keep hidden:
```

## REWORD — RELATED pointers name the current owner file; duplicates merged

`Cross-run progression -> META` is covered by the `Store Capital / Decoration / Job Mastery -> META_v2.8.0.md` line.

```text
## RELATED OWNERSHIP
Boss detailed rule -> BOSS
Final resolution -> FINAL_EXPEDITION
Cross-run progression -> META
```

```new
Boss detailed rule -> BOSS_v2.8.0.md
Final resolution -> FINAL_EXPEDITION_v2.8.0.md
```
