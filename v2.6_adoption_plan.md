# GUILD24 v2.6.0 Adoption — APPROVED  Plan

> **기반 소스**: GUILD24 v2.5.0 Production (`dist/`, `design_ssot/`)
> **작업 성격**: APPROVED IMPLEMENTATION PLAN. 구현 실행 기준.
> **인터뷰 및 최종 확정 사항**: 20개 질문 및 최종 Handoff 사전 확정사항(1~20항목) 전수 반영 완료.

---

## 1. Source / Design Gap

### 1.1 부상 / 중상
| 항목 | v2.5 Source | v2.6 Design |
|------|-----------|-------------|
| 부상 Stat penalty | **Flat**: combat -3, survival -5 | **%**: 투력 -15%, 강인함 -20% |
| 중상 Stat penalty | **Flat**: combat -6, survival -10 | **없음** (recovery 중 방문 불가이므로) |
| 부상 계산 순서 | (base + equip + trait) → flat 차감 → Item | **(base + equip) × NPC-side modifier** → Item |
| 중상 회복 후 | 중상(2) → 부상(1) | **중상 → 건강** (injury=0 직행) |
| 피로/기동 대상 | 기동만 `-fatigue*0.4` (Flat) | 기동 -10%/-25%, **정신 -10%/-25%** (%) |

### 1.2 피로
| 항목 | v2.5 Source | v2.6 Design |
|------|-----------|-------------|
| 획득량 | 결과 무관 일괄 +3 | 성공/대성공 +1, 퇴각 +2, 부상 +3, 중상 0, 사망 0 |
| 아침 자연 회복 | -2/일 | **삭제** |
| Stat penalty | mobility -= fatigue*0.4 (Flat) | 0~9: 없음, 10~19: 기동·정신 -10%, 20: -25% |
| 음식·음료 피로 회복 | 없음 | max(0, finalFoodDrinkSupply - requiredSupply) |

### 1.3 NPC Wallet
| 항목 | v2.5 Source | v2.6 Design |
|------|-----------|-------------|
| 첫 방문 | 90 + Lv×18 + rng(0,75) | **100 + Lv×8 + rng(0,60)** |
| 재방문 | 위 + money×0.28 | **이전 Persistent 100%** + Lv×8 + rng(0,60) |
| 100G 기본 지급 | 매 방문(walletBase=90) | **최초 방문에만** |
| Cap | 1,800G | **2,000G (Persistent Wallet 상시)** |
| loot base | 35 + Day×8 | **70 + Day×7** |
| loot Trait | greed +30%, lucky +7%, unlucky -7%, careful -8%, coward -12% | greed +30%, **coward -15%**, **careful -10%**, lucky/unlucky 삭제 |

### 1.4 Trait Pool
| 항목 | v2.5 Source | v2.6 Design |
|------|-----------|-------------|
| 총 수 | 30 | **37** |
| 삭제 | — | 행운아, 불운아 |
| 변경 | 허세(showoff) | **거짓말쟁이(liar)** |
| 신규 | — | 정직한, 금수저, 민감체질, 잔재주꾼, 몸치, 장비관리, 서투른, 내열성, 약시 |
| 상호배제 | 9쌍 | **16쌍** |

### 1.5 상품 해금 / 가격
| 항목 | v2.5 Source | v2.6 Design |
|------|-----------|-------------|
| 프리미엄 도시락 | 항상 해금 | **계정 D10 영구 해금** + Run D10 gate |
| 세계수 생환부적 | 항상 해금 | **계정 D14 영구 해금** + Run D14 gate |
| 황금 1+1 쿠폰 | buy=1000, sell=2000 | **buy=500, sell=1000** |

### 1.6 UI / Copy
| 항목 | v2.5 Source | v2.6 Design |
|------|-----------|-------------|
| 폭식 | 폭식 / 폭식의 마왕 글러트니 | **탐식 / 탐식의 마왕 글러트니** |
| 초기 자금 표시 | 1,200G · 24칸 | **1,000G · 18칸** |
| 발주 교환권 설명 | 30G부터 | **50G부터** |
| 전리품 | 전리품 | **NPC 소지금 획득** |
| Night 건너뛰기 | 건너뛰기 + 다음 + 전체 건너뛰기 | **다음 + 전체 건너뛰기만** |

---

## 2. 파일·시스템별 최소 변경

### 2.1 `dist/data/catalog.js`

#### Trait 변경 (Core Stat % 확정)
- **삭제**: `lucky`, `unlucky` 제거 (hidden luck 참조 0 구현)
- **변경**:
  - `showoff` → `liar` (거짓말쟁이): `{}`는 유지, 내부 ID와 name 변경
  - `brave`: fear `9→6`
  - `pyrophobia`: fire `-9→-6`
  - `sharpeye`: dark `6→4`, whiteout `6→4`
  - `antitoxin`: poison `8→6`
  - `coldhand`: cold `-9→-6`
  - `coward`: combat `-3` 제거, fear `-6` 추가, escape `0.19` 유지, loot `-0.12→-0.15`
  - `careful`: injuryRisk `-0.04` 유지, loot `-0.08→-0.10`. `injuryRisk`는 부상/중상 확률에만 적용(사망 미적용).
  - `reckless`: 기존 combat `+7` (Flat) 제거 → **`combat +10%`** (% 전환). `injuryRisk` 부상 확률에만 적용.
  - `grit`: `injuredCombat:6` 대체 → 일반 부상 combat -15%를 대체하여 **`combat +20%`** 적용. (survival -20%는 유지, fatigue +1 유지).
  - `stamina`: `fatigue:-1` 유지 (원정 결과 피로 -1)
  - `weary`: `fatigue:1` 유지 (원정 결과 피로 +1)
  - `frail`: 기존 survival `-5` (Flat) 제거 → **`survival -10%`** (% 전환).
- **신규 Trait** (9개):
  - `honest` (정직한, **mixed**): `{loyaltyBonus:1, overchargeBias:-0.10}`
  - `rich` (금수저, positive): `{visitGold:50}` — 실제 방문 1회당 소지금 +50G (arrive()에서 1회 한정 적용)
  - `sensitive` (민감체질, negative): `{poison:-6}`
  - `nimble` (잔재주꾼, positive): `{bind:4, mire:4}`
  - `clumsy` (몸치, negative): `{bind:-4, mire:-4}`
  - `maintain` (장비관리, positive): `{corrosion:6}`
  - `butterfingers` (서투른, negative): `{corrosion:-6}`
  - `heatproof` (내열성, positive): `{fire:6}`
  - `nearsight` (약시, negative): `{dark:-4, whiteout:-4}`

#### 상호배제 (`relics.js`)
기존 8쌍 유지 + 신규 8쌍 추가 = **총 16쌍**:
```
기존 유지: brave↔coward, eater↔small, careful↔reckless, frugal↔impulse,
          strong↔frail, collector↔thrifty, stamina↔weary, social↔shy
추가(8):   frail↔mender, antitoxin↔sensitive, nimble↔clumsy,
           maintain↔butterfingers, heatproof↔pyrophobia,
           sharpeye↔nearsight, aloof↔coldhand,
           honest↔liar (정체성 상호배제 추가)
```

---

### 2.2 `dist/systems/dungeon.js`

#### `prepare()` — 계산 순서 재구성 (A~F)
```
A. NPC Base + Equipment 확보
B. 현재 Bag Item 평가 (Sold Item Stat, final Supply contribution, finalFoodDrinkSupply 산출. NPC Stat 미합산)
C. fatigueRecovery = max(0, finalFoodDrinkSupply - (requiredSupply||0))
   effectiveFatigue = max(0, n.fatigue - fatigueRecovery) 산출
D. Base + Equip 영역에 Trait/Injury/Fatigue 등 NPC-side % modifier 적용
   - Core Stat Trait % (reckless +10%, frail -10%) 적용
   - 부상 % (투력 -15% (악바리 +20%), 강인함 -20%) 적용
   - 피로 % (effectiveFatigue 기준, 기동/정신 10~19: -10%, 20: -25%) 적용
E. Sold Item Stat을 최종 Stat에 합산 (Item Stat에는 NPC-side % 적용 않음)
F. 기존 Supply Burden / Hazard 계산 진행
```

#### Hidden Luck 축 완전 제거
- `prepare()`: luck 변수 제거
- `resolve()`: combat noise 및 escapeChance의 `e.luck` 제거
- 기타 active hidden luck 참조 0.

#### `resolve()` — 피로 확정 Commit
- `n.fatigue`는 `resolve()` 시에만 1회 Commit. (미리 Preview에서 수정 안함)
- `effectiveFatigue + Outcome fatigue + Trait fatigue modifier` 결과값을 0~20 clamp.
- Outcome baseline: 성공/대성공 +1, 퇴각 +2, 부상 +3, 중상 0, 사망 0.
- 중상의 경우 baseline 0 에 Trait fatigue modifier는 정상 적용.

---

### 2.3 `dist/systems/shop.js` 및 `dist/systems/run.js`

#### Wallet 공식 교체 및 금수저 (`shop.js`)
- `morning()` 공식에서는 금수저 `+50G`를 넣지 않음.
- **Persistent Wallet Cap 2,000G 상시 유지**.
- `arrive()` 내 실제 방문 시 1회 한정하여 금수저 `+50G` 적용 (re-render 등 중복 불가).
- **Event temporary purchase budget**: Persistent Wallet과 완전 분리(Cap 미포함, 이월 안됨). 단, Event budget 산출 기준이 되는 Persistent Wallet 값은 금수저 +50G 및 Cap이 반영된 최종값을 따르도록 계산 순서 동기화.

#### 거짓말쟁이(liar) 및 정직한(honest)
- `liar`: 50% 확률로 실제 배정 Gate 변경 (claimedDestination 유지).
- `honest`: 정가/할인 구매 성공 시 loyalty +1, 바가지 구매의사 -10%p.

#### ORDER Reroll 분리 (실제 Source 기준 배치)
- `run.js`: `reroll()`, `finishOrder()`
- `shop.js`: `confirmOrder()`, `open()`
- Flow: 선택 발주 확정(Inventory 반영, Phase 유지) → 필요 시 Reroll (미확정 cart 초기화, 전체 Offer 교환, 비용 1회 차감, 확정된 인벤토리 유지) → 영업 시작 시 전환.
- 내역 유지: 오늘 확정 발주 내역/누적 금액 유지, Reroll 누적 비용 익일 Reset 로직 등 기존 규칙 유지.
- Scroll / Focus 안정화 처리(기존 재사용).

---

### 2.4 `dist/systems/meta.js` & `dist/systems/save.js` (SAVE v7)

#### D10/D14 해금 로직
- D10: PremiumLunch / D14: WorldTreeAmulet
- Offer 생성 전 unlock 처리. 해당 Day Offer 풀부터 등장. Toast 계정 단위 1회 표출.
- 계정에 해금되어도, 새 Run에서 Day 조건 충족 전에 등장 금지.

#### SAVE v7 전체 갱신
- KEY=`guild24.save.v7`, LEGACY `v6` 추가, write/export/valid/run.version 모두 `7`.
- **Migration 없음**: v6 로드 시 새 버전 게임 안내(이어서 로드 금지), 기존 원본 자동 삭제하지 않음. 데이터 실제 삭제는 User가 Full Reset 수행 시에만.
- `account.unlocks` validation 추가.

#### Full Data Reset vs Abandon
- **Full Data Reset**: Run, Account/Meta, Tutorial, D10/D14 해금/Toast 모두 Fresh 초기화.
- **현재 지점 포기(Abandon)**: Run만 초기화. Account/Meta, Tutorial, 해금 상태는 유지.

---

### 2.5 `dist/ui/app.js` 및 UI 변경

#### SALE Layout 및 Screenshot 규칙 적용
첨부된 User Screenshot을 실제 Layout Reference로 활용하되, 확정 정보 위계를 준수.
- **위계**: 예상 목적지 > Bag/슬롯 > 전투 전망 > 환경 전망 > 부상/피로 > NPC Wallet
- **Desktop**: 캐릭터 우측 상단 빈 공간 활용. 왼쪽 가방 크기 대폭 확대. 가방 우측(또는 하단)에 '전투 전망', 그 아래 '예상 목적지' 배치 (기존 하단 위치는 삭제).
- **Mobile**: 상단(캐릭터/상태) 세로 길이 축소 (Artwork 임의 crop 금지). 가방 확대 및 핵심 터치영역 확보. 가방 아래 '예상 목적지 작게' 배치(터치 시 환경 압박 표출). 지난 원정 정보 바로 아래에 '전투 전망' 배치. 환경 정보 중복 제거.
- **진행**: Desktop/Mobile 저수준 Wireframe을 먼저 제시 후 승인받아 구현.

#### SALE STAT SOURCE UX
- 변경된 Stat의 노란색 강조 유지.
- Stat 하단에 실제 적용된 **Source 이름만** 작게 표시 (유리: 초록, 불리: 빨강). 미적용 Source 표시 안함.
- Stat 터치 시: Source / 실제 적용값 / 계산 과정을 담은 모달/Breakdown 표출.
- NPC Detail 창: 실제 부상 효과, 현재 피로/적용 penalty, 남은 휴식일, 회복 방법 표시.

#### NIGHT UI
- `건너뛰기` 삭제, `다음` / `전체 건너뛰기` 유지.
- Result 정보: 원정 fatigue gain, 최종 fatigue, 현재 injury penalty, severe 남은 기간, **NPC 소지금 획득**.
- **Runtime Causality**: Item/Trait이 실제로 결과를 방지/완화/변경한 판정 근거가 있을 때만 원인으로 표시 (단순 보유 여부 표출 금지).

#### MENU / SETTINGS
- Menu Top-level: Sound Toggle, Full Data Reset 제거. `설정 · 저장` → `설정` 변경.
- Settings 내부: 저장 관련, Sound, Full Data Reset 유지. '현재 지점 포기' 제거.

#### TARGETED COPY & PORTRAIT PRELOAD
- 전체 Copy 재작성 금지. Repository exact search로 1:1 치환.
- `폭식`→`탐식`, `전리품`→`NPC 소지금 획득`, `탈출 보정`→`탈출 확률` 등 지정 문자열 갱신.
- SALE 렌더 시 다음 손님 Portrait Preload (`new Image().src = ...`).

---

## 3. QA / Acceptance

최종 반영 QA 리스트:
1. Active Trait Pool = 37종
2. Mutual Exclusion = 16쌍 정합성
3. honest ↔ liar 동시 생성 불가 확인
4. honest direction = MIXED 확인
5. reckless combat = +10%, frail survival = -10% 확인
6. direct Core Stat Trait Flat 잔존 없음 확인
7. Core Stat Trait % 계산 순서(A~F 준수) 정확성
8. hidden luck active reference = 0 확인
9. 금수저 방문당 정확히 +50G 확인 (중복 렌더 방지)
10. Wallet Cap 2,000G 모든 Persistent 증가 경로 확인
11. Event temporary budget의 Cap 외부/비이월 로직 확인
12. requiredSupply=0 인 T1 Fatigue Recovery 동작 확인
13. SALE Preview가 persistent fatigue 덮어쓰지 않음 확인
14. 중상 fatigue baseline 0 + Trait modifier 적용 확인
15. 부상/피로 %가 Sold Item Stat에 NPC-side로 잘못 재적용되지 않음 확인
16. 악바리 combat +20%가 일반 부상 combat -15% 대체함 확인
17. Boss Final modifier Scope / 순서 불변 확인
18. Night Runtime causality 명확성 확인
19. ORDER 확정 → 전체 Offer Reroll → 재확정 흐름 확인
20. Reroll 비용 익일 누적 Reset, Scroll/Focus stabilization 유지 확인
21. v7 Save (write/read/export/validation) 일관성 확인
22. Full Reset vs Abandon 초기화 범위 명확성 확인
23. D10/D14 Unlock 조건 및 Toast 1회 동작 확인
24. Night 2-button (다음, 전체 건너뛰기) flow 확인

---

## 4. 구현 후 Balance Simulation

구현 완료 후 `simulation.js` 수정 및 측정. (Production 수치 자동 변경 금지)

**측정 항목:**
- Wallet D1/D10/D20/D30 분포, Cap 도달률
- Expedition wallet gain 영향, 탐욕/겁쟁이/신중함/금수저 영향
- 정직한 바가지 성공률 -10%p 체감률
- Fatigue 10/20 도달 빈도 및 초과 Supply 관리 가치
- Hazard ±6 / ±4 실효성
- Core Stat Trait (무모함 +10%, 허약함 -10%) 실효성
- **전체 영향력 위계 측정 (동일조건 Ablation)**

**영향력 목표 위계:**
> NPC 기본체급 / Level / Growth / 장비 **>** 판매 Item **>** 점포지원 **>** Event **>** Trait

**보고 방식:**
- 수치, 분포, 빈도, 위계 위반 여부, 조정 후보만을 보고 (승인 대기).

---

## 5. DESIGN SSOT VERSIONING

- `SPEC_INDEX_v2.6.0.md` 새 Current Manifest 생성.
- 실제 내용이 변경된 Owner Spec / QA 문서만 `_v2.6.0.md` 로 생성 (중복 작성 금지).
- 변경이 없는 Owner/QA 문서는 기존 `v2.5.0` 파일을 참조 유지.
